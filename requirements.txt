# Kafka client (works with Redpanda)
confluent-kafka==2.4.0

# Data
duckdb==1.0.0
deltalake==0.18.2
pandas==2.2.2

# Stream processing (Phase 2) — single-node local Spark
pyspark==3.5.1
delta-spark==3.2.0

# Dashboard (Phase 3)
streamlit==1.35.0
plotly==5.22.0

# Ingestion utilities
requests==2.31.0
python-dotenv==1.0.1

# Quality & testing
pytest==8.2.0
pytest-cov==5.0.0

# Football visualization (pins resolve numpy/scipy/streamlit conflict)
mplsoccer==1.4.0
numpy<2
scipy<1.14
